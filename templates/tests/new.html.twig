{% extends 'base.html.twig' %}

{% block title %}New Tests{% endblock %}

{% block body %}
    <div class="max-w-2xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-2xl">
        <div class="flex justify-between">
            <h2 class="text-2xl font-semibold text-gray-900 mb-6 text-center">Créer un nouveau test</h2>
            <a href="{{ path('app_tests_index') }}" class="inline-block mb-4 text-blue-600 hover:underline">← Retour à la liste</a>
        </div>
        {{ include('tests/_form.html.twig', {'delete_form': false}) }}
    </div>
{% endblock %}
