<nav class="bg-blue-600 p-4 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="relative flex items-center justify-between h-16">
            <!-- Mobile menu button-->
            <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
                <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-blue-800 focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
                    <span class="sr-only">Open main menu</span>
                    <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                    </svg>
                    <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                    </svg>
                </button>
            </div>

            <!-- Logo -->
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
                <div class="flex-shrink-0 text-white font-bold text-2xl">
                    BugBuster
                </div>
            </div>

            <!-- Links -->
            <div class="hidden sm:block sm:ml-6">
                <div class="flex space-x-4">
                    {% if app.user %}
                        <a href="{{ path('app_tests_index') }}" class="text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium transition duration-200 ease-in-out">Tests</a>
                        <a href="{{ path('app_pv_recettage_index') }}" class="text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium transition duration-200 ease-in-out">PV Recettage</a>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium transition duration-200 ease-in-out">Login</a>
                        <a href="{{ path('app_register') }}" class="text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium transition duration-200 ease-in-out">Register</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Mobile menu, toggle className based on menu state. -->
    <div class="sm:hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1">
            {% if app.user %}
                <a href="{{ path('app_tests_index') }}" class="text-white block px-3 py-2 rounded-md text-base font-medium">Tests</a>
                <a href="{{ path('app_pv_recettage_index') }}" class="text-white block px-3 py-2 rounded-md text-base font-medium">PV Recettage</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="text-white block px-3 py-2 rounded-md text-base font-medium">Login</a>
                <a href="{{ path('app_register') }}" class="text-white block px-3 py-2 rounded-md text-base font-medium">Register</a>
            {% endif %}
        </div>
    </div>
</nav>

<script>
    const menuButton = document.querySelector('button[aria-controls="mobile-menu"]');
    const mobileMenu = document.getElementById('mobile-menu');
    menuButton.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
    });
</script>
